"""
Member 1 -- Authentication & Project Setup. Not part of Member 3's
(Driver + Trip) submission -- stub only, so the folder exists and the
import contract is visible ahead of time.

Collections: users

APIs to implement here:
    POST /register
    POST /login
    GET  /profile

Business rules:
    - JWT authentication (flask_jwt_extended)
    - Role-based access control (roles in utils.constants.USER_ROLES)

See models/user.py for the shared document shape.
"""
